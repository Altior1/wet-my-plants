import { Text, View } from "react-native";

export default function AddPlantsPage() {
  return(
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
        <Text style={{ fontSize: 20, fontWeight: 'bold' }}>Add Plants Page</Text>
    </View>
  );
}